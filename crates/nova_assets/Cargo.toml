[package]
name = "nova_assets"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true }
bevy_asset_loader = { workspace = true }

[lints]
workspace = true

[features]
debug = []
